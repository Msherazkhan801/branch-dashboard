# TODO

## Task: Fix branch selection on specific branch pages

- [x] Add `fixedBranch` prop to `AddIncomeModal` (hide dropdown when set)
- [x] Add `fixedBranch` prop to `AddClassIncomeModal` (hide dropdown when set)
- [x] Add `fixedBranch` prop to `AddExtraExpenseModal` (hide dropdown when set)
- [x] Pass `fixedBranch={branchFilter}` from `DashboardContent` to the three modals
- [x] Run lint + typecheck (typecheck passes; only pre-existing lint warnings/errors remain)
